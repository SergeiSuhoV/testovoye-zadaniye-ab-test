import "./Finalize.scss";

import React from "react";

function Finalize() {
  return (
    <div className="finalize">
      FINALIZE
      <nav className="finalize__nav-back">Back</nav>
    </div>
  );
}

export default Finalize;
